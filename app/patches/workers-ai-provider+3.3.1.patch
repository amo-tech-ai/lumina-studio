diff --git a/node_modules/workers-ai-provider/dist/index.mjs b/node_modules/workers-ai-provider/dist/index.mjs
index 6bd4682..fa18376 100644
--- a/node_modules/workers-ai-provider/dist/index.mjs
+++ b/node_modules/workers-ai-provider/dist/index.mjs
@@ -688,10 +688,13 @@ function getMappedStream(response, salvageContext) {
 			if (choiceFinishReason != null) finishReason = mapWorkersAIFinishReason(choiceFinishReason);
 			else if (directFinishReason != null) finishReason = mapWorkersAIFinishReason(directFinishReason);
 			const nativeResponse = chunk.response;
+			let nativeText;
 			if (nativeResponse != null && nativeResponse !== "") {
 				const responseText = String(nativeResponse);
-				if (responseText.length > 0) if (bufferContentForSalvage) contentBuffer += responseText;
-				else {
+				if (responseText.length > 0) {
+					nativeText = responseText;
+					if (bufferContentForSalvage) contentBuffer += responseText;
+					else {
 					if (reasoningId) {
 						controller.enqueue({
 							type: "reasoning-end",
@@ -711,6 +714,7 @@ function getMappedStream(response, salvageContext) {
 						id: textId,
 						delta: responseText
 					});
+					}
 				}
 			}
 			if (Array.isArray(chunk.tool_calls)) {
@@ -741,8 +745,9 @@ function getMappedStream(response, salvageContext) {
 					});
 				}
 				const textDelta = delta.content;
-				if (textDelta && textDelta.length > 0) if (bufferContentForSalvage) contentBuffer += textDelta;
-				else {
+				const isDuplicateDelta = nativeText !== undefined && textDelta === nativeText;
+				if (textDelta && textDelta.length > 0 && !isDuplicateDelta) if (bufferContentForSalvage) contentBuffer += textDelta;
+				else if (textDelta && textDelta.length > 0 && !isDuplicateDelta) {
 					if (reasoningId) {
 						controller.enqueue({
 							type: "reasoning-end",
diff --git a/node_modules/workers-ai-provider/src/streaming.ts b/node_modules/workers-ai-provider/src/streaming.ts
index f1937cf..7e983aa 100644
--- a/node_modules/workers-ai-provider/src/streaming.ts
+++ b/node_modules/workers-ai-provider/src/streaming.ts
@@ -165,9 +165,14 @@ export function getMappedStream(
 
 				// --- Native format: top-level `response` field ---
 				const nativeResponse = chunk.response;
+				// IPI-771 · CF-MIG-230-W1 — dual-format SSE text dedup:
+				// dual-format SSE may set both `response` and
+				// `choices[0].delta.content`. Skip OpenAI delta only when identical.
+				let nativeText: string | undefined;
 				if (nativeResponse != null && nativeResponse !== "") {
 					const responseText = String(nativeResponse);
 					if (responseText.length > 0) {
+						nativeText = responseText;
 						if (bufferContentForSalvage) {
 							contentBuffer += responseText;
 						} else {
@@ -222,7 +227,9 @@ export function getMappedStream(
 					}
 
 					const textDelta = delta.content as string | undefined;
-					if (textDelta && textDelta.length > 0) {
+					const isDuplicateDelta =
+						nativeText !== undefined && textDelta === nativeText;
+					if (textDelta && textDelta.length > 0 && !isDuplicateDelta) {
 						if (bufferContentForSalvage) {
 							contentBuffer += textDelta;
 						} else {
