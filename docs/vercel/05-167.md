> **Status: DONE — 2026-06-24.** All 3 blockers resolved and deployed. One env var action required before live e2e test (see checklist).

---

Yes, **IPI2-167 is mostly correct**, but it has **3 important errors to fix before Claude builds it**.

## Main errors

| Issue                           | Problem                                                                       | Correct fix                                                                          |
| ------------------------------- | ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| `returns {draftId, claimToken}` | ❌ Wrong/security risk. Token must not be JSON-returned.                       | Return `{ draftId }` only. Token must be httpOnly cookie.                            |
| “Mastra tool calls edge fn”     | ⚠️ Risky for cookies. Server tools cannot easily set browser httpOnly cookie. | Use frontend tool → `/api/marketing-lead` Next route → edge fn → forward/set cookie. |
| “PR #48 open / not pushed”      | ❌ Stale. Earlier status said PR #48 merged and Supabase pushed.               | Update context: DB schema already deployed.                                          |

## Corrected task shape

```text
IPI2-167 · WEB-015.8 — Lead capture workflow

Frontend CopilotKit tool:
captureLeadDraft(payload)

Calls:
POST /api/marketing-lead

Server route:
- validates payload
- calls capture-lead edge function
- receives draftId
- forwards or sets httpOnly claim_token cookie
- returns { draftId } only

Never expose:
- claimToken in JSON
- claimToken in URL
- claimToken in localStorage
- claimToken to JS
```

## Best implementation rule

Do **not** give the public agent direct Supabase writes.

Use this path:

```text
Chat widget
→ frontend tool captureLeadDraft
→ /api/marketing-lead
→ capture-lead edge function
→ Supabase lead_intake_drafts
→ httpOnly claim_token cookie
```

## Verdict

Proceed, but update the issue first:

```text
Remove “returns {draftId, claimToken}”
Replace with “returns {draftId}; sets claim_token as httpOnly cookie only”
```
The forensic report is useful, but I would **not accept it as 100% accurate without verification**.

### Verified findings: ✅

These are likely correct and should be addressed:

| Item                                                           | Verdict             |
| -------------------------------------------------------------- | ------------------- |
| Agent prompt does not explicitly call `capture_lead`           | ✅ Likely valid      |
| `capture_lead` frontend tool already exists                    | ✅ Good architecture |
| `/api/marketing-lead` proxy exists                             | ✅ Good architecture |
| Direct client → Supabase writes avoided                        | ✅ Correct           |
| Tool name mismatch risk (`capture_lead` vs `captureLeadDraft`) | ✅ Valid warning     |
| Status mismatch (`draft` vs `ready`)                           | ✅ Needs review      |

---

### Findings I would verify before acting: ⚠️

#### B2 — "PR #48 not deployed, tables don't exist"

This is the biggest claim.

The report says:

> DB schema not on remote — PR #48 tables don't exist

But earlier project status indicates:

* IPI2-160 completed
* capture-lead already deployed
* chatbot runtime deployed
* Next.js production site live

Before changing code, verify:

```bash
cd /home/sk/ipix

supabase migration list

supabase db remote commit
```

Or check directly:

```sql
select table_name
from information_schema.tables
where table_schema='public'
and table_name in (
  'lead_intake_drafts',
  'chatbot_conversations',
  'chatbot_messages',
  'chatbot_events'
);
```

If those tables exist remotely, B2 is false.

---

#### B3 — Returning claimToken

I disagree with the report's implementation recommendation.

Current acceptance says:

> return {draftId, claimToken}
> set httpOnly claim_token cookie

That is contradictory.

If claim_token is security-sensitive:

```text
GOOD:
Edge Function
→ returns draftId
→ Next.js route sets httpOnly cookie

BAD:
Edge Function returns claimToken JSON
→ browser receives token
→ JS can access token
```

For IPI2-167, I would update the spec:

```text
Return:
{
  draftId
}

Set:
claim_token
as httpOnly cookie only
```

Never expose claimToken to JavaScript.

---

### Architecture recommendation

For IPI2-167, use:

```text
CopilotPopup
    ↓
capture_lead frontend tool
    ↓
/api/marketing-lead
    ↓
capture-lead edge function
    ↓
lead_intake_drafts
    ↓
httpOnly claim_token cookie
```

Not:

```text
Agent Tool
 ↓
Direct Supabase write
```

---

## What Claude should actually build

### IPI2-167 · WEB-015.8 — Lead Capture Workflow

#### Required

* Add prompt instruction to call `capture_lead`
* Ensure payload contains:

  * name
  * email
  * brand_name
  * service_interest
  * goal
  * budget_range
  * timeline
  * website
  * project_summary
* Upsert existing draft by:

  * anon_id OR email
* Set status = `ready`
* Emit `lead_captured`
* Return `draftId`
* Set httpOnly `claim_token` cookie in `/api/marketing-lead`

#### Verify

* `lead_intake_drafts` exists remotely
* `chatbot_conversations` exists remotely
* `chatbot_events` exists remotely
* `claim_lead_draft()` RPC exists remotely

#### Do NOT build

* URL grounding (IPI2-166)
* Intent classification (IPI2-165)
* Brand intake prefill (IPI2-168)
* Analytics (IPI2-169)

---


### Priority order

1. **Verify remote DB actually contains the tables**
2. **Fix prompt → tool invocation**
3. **Implement cookie flow**
4. **Add tests**
5. **Then start IPI2-168**

If I were reviewing this ticket, I would estimate **IPI2-167 is already 70–80% implemented and mostly needs wiring + cookie handling, not a new architecture.**


Linear updated. Here's the forensic verdict:

---
IPI2-167 is 80% done on disk â but blocked by 3 bugs before any lead saves.

What surprised me (stale spec)

The spec said "Mastra tool on the agent" â wrong. The widget already has useFrontendTool capture_lead wired correctly. CopilotKit v2 + Mastra docs confirm frontend tools is the right pattern. The full chain exists:

Agent calls capture_lead  â  useFrontendTool handlad
â  Next.js Zod proxy  â  capture-lead edge fn  â

3 blockers preventing leads from saving

âââââââ¬ââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââ
â  #  â                                Blocker                         Fix                      â
âââââââ¼ââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââ¼âââââââââââââââââââââââââââââââââââââââââââââââ¤
â ð´  â Agent prompt never calls the tool â ## When ready_to_submit says      â Add one line to prompt: Call the             â
â B1  â "tell the visitor", not "call capture_lead capture_lead tool with all collected fields. â
â     â never fires.                                                          â                                              â
âââââââ¼ââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââ¼âââââââââââââââââââââââââââââââââââââââââââââââ¤
â ð´  â DB schema not on remote â PR #48 tables do Merge PR #48 + run npm run supabase:push     â
â B2  â returns 500.                                                          â                                              â
âââââââ¼ââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââ¼âââââââââââââââââââââââââââââââââââââââââââââââ¤
â ð´  â claim_token never returned â stored in DB  Return claimToken from edge fn; set httpOnly â
â B3  â IPI2-168 can't claim the draft.                                       â  cookie in /api/marketing-lead proxy         â
âââââââ´ââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââââ´âââââââââââââââââââââââââââââââââââââââââââââââ

Quick wins (not blocked)

- B1 is a 2-line prompt fix â can ship in minutes
- B2 needs supabase:push â requires confirming the remote migration password is set
- B3 is a small edge fn + proxy change

Want me to fix B1 (prompt) now since it's the fastest unblock, or tackle all three together?