---
description: "Alias for /efficient — ranked implementation paths before coding."
argument-hint: "[task|plan|branch|api|feature] <IPI-XXX|path|description>"
allowed-tools: ["Bash", "Glob", "Grep", "Read", "SemanticSearch", "Task"]
---

# /discover — alias for `/efficient`

**Use `/efficient` as the canonical command.** Same workflow, same arguments.

When invoked as `/discover`, follow **`.claude/commands/efficient.md`** Phases 0–6 exactly. End with the `/efficient` stop gate.

**Arguments:** `$ARGUMENTS`
