# Create agent-priority Linear issues

Use on **Cursor Desktop** after Linear MCP is authenticated (Cloud cannot OAuth Linear).

1. Read `docs/linear/issues/CURSOR-DESKTOP-CREATE-LINEAR-ISSUES.md`
2. Prefer: `infisical run --env=dev -- node scripts/linear-create-agent-priority-issues.mjs`
3. Or create/update via Linear MCP from the five specs in `docs/linear/issues/agent-priority-README.md`
4. Reuse **IPI-156** (update only). Do not file Support/Postiz/Apify/OpenClaw agents.
5. Reply with the five Linear URLs in execution order.
