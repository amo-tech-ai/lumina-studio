# Roadmaps

View Linear roadmaps.

```bash
linear-cli rm list
linear-cli rm list --output json
linear-cli rm get ROADMAP_ID
linear-cli rm get ROADMAP_ID --output json
```

| Flag | Purpose |
|------|---------|
| `--output json` | JSON output |
| `--compact` | No formatting |
