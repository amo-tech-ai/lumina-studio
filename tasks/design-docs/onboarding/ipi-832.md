# Audit — **IPI-832 · ONB2-DB-001 — Onboarding Sessions, Atomic Materialization RPC, and Database Authorization Proof**

**Linear:** [IPI-832](https://linear.app/amo100/issue/IPI-832/ipi-832-onb2-db-001-onboarding-sessions-atomic-materialization-rpc-and)  
**Status:** ✅ Done · **PRs:** [#701](https://github.com/amo-tech-ai/lumina-studio/pull/701) A (`91bf0395`), [#703](https://github.com/amo-tech-ai/lumina-studio/pull/703) B (`f06c7917`) · **Parent:** IPI-831  
**Audit date:** 2026-08-01 · **Post-merge:** [`pr-701-merge-record.md`](./pr-701-merge-record.md) · [`pr-701-follow-up.md`](./pr-701-follow-up.md) · harden tests [IPI-893](https://linear.app/amo100/issue/IPI-893)

Legend: 🟢 correct / verified · 🟡 partial / risk · ⚪ missing / deferred · 🔴 wrong / blocker

---

## Plain English

Today the v2 wizard forgets everything on refresh, and the old create helper can orphan orgs on double-tap. This task adds a draft session table + one atomic RPC that turns a draft into **exactly one** org + brand, with RLS so nobody else can see your draft.

---

## Scorecard

| Dimension | Score | Dot | Note |
| --- | ---: | --- | --- |
| Spec accuracy (current broken state) | **95%** | 🟢 | Two-round-trip + random slug + unchecked delete — confirmed |
| Schema / RPC design quality | **88%** | 🟢 | Invoker, pre-gen UUIDs, unique key — strong |
| “Done looks like” vs schema | **72%** | 🟡 | “Unique per user/org” ≠ unique `(user_id, idempotency_key)` |
| Dependency graph (Linear relations) | **65%** | 🟡 | `blockedBy` empty; still **blocks canceled IPI-808** |
| Slice plan (A/B/C) | **90%** | 🟢 | One concern per PR — correct |
| QA / test plan | **78%** | 🟡 | Race test correctly needs QA; pgTAP wording over-couples to 829 |
| Will succeed if executed as written? | **75%** | 🟡 | High if slices followed; fails if A+B+C mixed or race skipped |
| Overall task health | **80%** | 🟢 | Best data ticket — start **now** (∥ OAuth) |

**Not 100% correct** — wording gaps, stale relation to IPI-808, SLA already breached, “blocked by 829” overstated for slices A/B.

---

## What Linear says vs reality

| Claim | Reality | Dot |
| --- | --- | --- |
| `onboarding_sessions` does not exist on prod | ✅ Confirmed (prior audit + SQL; no materialize fns) | 🟢 |
| No `%materialize%` RPC | ✅ | 🟢 |
| `createOrgAndBrand` = 2 inserts + unchecked org delete | ✅ `onboarding.ts` lines ~81–92 | 🟢 |
| Slug ends in `Math.random()` | ✅ | 🟢 |
| Column is `brands.brand_url` (not org website) | ✅ Spec warns correctly | 🟢 |
| Must pre-generate UUIDs (IPI-809 RETURNING trap) | ✅ Critical and correct | 🟢 |
| Unique `(user_id, idempotency_key)` prevents phantom double-insert | ✅ Correct Postgres reasoning | 🟢 |
| “Active draft unique per user/org” (Done short) | 🟡 Schema allows multiple drafts if keys differ — need app rule or partial unique | 🟡 |
| Blocked by IPI-829 for A/B | 🔴 Body says A/B can ship without QA; Linear `blockedBy` empty (good) but prose still confuses | 🟡/🔴 |
| Blocks IPI-835 | ✅ Correct | 🟢 |
| Blocks IPI-808 | 🔴 **IPI-808 is Canceled** — stale edge | 🔴 |
| SLA | 🔴 Already breached (breachesAt 2026-07-28) | 🔴 |

---

## Errors · red flags · failure points

| # | Severity | Issue | Fix |
| --- | --- | --- | --- |
| 1 | 🔴 | **Stale Linear relation:** still **blocks** canceled **IPI-808** | Remove `blocks` → IPI-808; only block **IPI-835** (+ maybe 836 via 835) |
| 2 | 🔴 | **“One active draft” not enforced in SQL** | Add partial unique index `unique (user_id) where status = 'draft'` **or** document that idempotency_key is stable per browser (localStorage) and get-or-create uses that single key |
| 3 | 🔴 | **Mixing slices / skipping race test** | Ship A (migration) → B (module) → C (pgTAP + race). Never claim Done without race when `QA_DATABASE_URL` exists |
| 4 | 🟡 | **pgTAP listed as needing 829** | Run `008` against any DB that has the migration (QA preferred). Don’t block slice A on full staging catch-up |
| 5 | 🟡 | **Staging `wtuhdynujhszsbwxlbdi` ~54 migrations behind** | **IPI-829** still needed for honest race/Playwright; don’t pretend staging is QA today |
| 6 | 🟡 | **RPC doesn’t persist `draft_answers` / screen** | OK for 832 — but B must expose get-or-create + update helpers or 835 will invent a second API |
| 7 | 🟡 | **Module `git mv` trap** (`onboarding.ts` vs `onboarding/`) | Keep the ticket’s warning; one commit for mv + imports |
| 8 | 🟡 | **Existing tests must be updated, not deleted** | `onboarding.test.ts` random slug; orchestration grep for `createOrgAndBrand` |
| 9 | ⚪ | **No get-or-create session RPC/API spelled as AC** | Add AC: upsert draft by idempotency key; resume returns same `id` + `current_screen` |
| 10 | ⚪ | **Org `type` / planner trigger side effects** | Spec mentions AFTER triggers — add one pgTAP that materialize leaves valid `org_members` row |

---

## What’s missing from the ticket

- Explicit **idempotency_key lifecycle** (who generates? localStorage? cookie? server?)
- **Autosave contract** for `draft_answers` + `current_screen` (even if thin — 835 depends on it)
- Partial unique for **one draft per user** (or justified multi-draft product rule)
- Cleanup of **blocks IPI-808**
- Grant/revoke checklist mirrored from IPI-809 lessons (ticket has revoke — good; keep in review)
- Mention that **v2 `/onboarding` does not call this yet** — wiring is 835 (avoid scope creep)

---

## Critical fixes (do these before / while coding)

1. **Clarify draft uniqueness** in Linear AC (partial unique **or** single stable key).  
2. **Drop blocks→IPI-808**; keep blocks→IPI-835.  
3. **Start slice A immediately** — do not wait for IPI-837 merge.  
4. **Wire `QA_DATABASE_URL` path** (IPI-829) before claiming race Done — but ship A/B first.

---

## Best path to complete (efficient)

```text
Now (∥ IPI-837 smoke/merge):
  Slice A — migration + RLS + RPC + grants     → own PR
Then:
  Slice B — git mv module, replace createOrgAndBrand with RPC
Then (needs QA DB):
  Slice C — 008 pgTAP + Promise.all race test
```

| Approach | Verdict |
| --- | --- |
| Follow A/B/C as written | 🟢 Best |
| One mega PR migration+code+tests | 🔴 Violates one-concern; hard rollback |
| DEFINER RPC “to make RLS easier” | 🔴 Reject — ticket correctly forbids copying `commit_shoot_draft` |
| Client-only localStorage drafts | 🔴 Doesn’t fix orphan org create |

**Ponytail:** smallest success is **slice A on prod** (additive, rollback = drop). Don’t build abandonment jobs, version columns, or CopilotKit persistence.

---

## Success odds

| Path | Odds |
| --- | --- |
| A → B → C with race on real QA | 🟢 **~80%** |
| A+B only, skip race | 🟡 **~55%** — silent double-materialize under load |
| Wait for perfect staging clone before A | 🔴 Delays for no gain |
| Expand into crawl/Realtime in same PR | 🔴 Becomes 835; will fail review |

---

## Bottom line

🟢 **Technical design is excellent and still accurate on prod.**  
🟡 **Not 100%:** “one draft” AC, stale IPI-808 edge, 829 over-coupling, breached SLA.  
🔴 **Failure modes:** skip unique/race reasoning; mix concerns; invent DEFINER.  
**Start slice A now** — parallel with finishing **IPI-837**.
