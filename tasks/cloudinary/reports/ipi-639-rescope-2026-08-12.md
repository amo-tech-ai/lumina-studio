# IPI-639 · CLD-APPROVAL-001 — Rescope: No asset_approvals for v1

**Date:** 2026-08-12
**Main:** `47ca470d7` (includes IPI-441 `d5ae00bf1` + IPI-64 `81aba5f04`)
**Cloud:** `dzqy2ixl0` · **Preset:** `ipix-signed-upload` signed/authenticated · **Webhook:** `https://www.ipix.co/api/assets/cloudinary/webhook` (upload+delete+moderation)

## Decision — corrected per review (keep business separate)

**RESCOPE → CLOSE WITH PROOF for v1 provider moderation, but business approval remains separate:**

- **Provider moderation** (`moderation: manual` → `pending`/`approved`/`rejected` via Cloudinary Media Library) → `cloudinary_assets.moderation_status` + `asset_events` `kind: moderated` (provider, `reason: manual`, no `actorId`, `request_id` from Cloudinary) — **this PR's scope, proven above**
- **Business approval** (operator clicks Approve in iPix for `v1`) → **separate** `asset_approvals` or `asset_events` with `actorId` (who) + `reason` (comment) + `version` exact — **not in this docs rescope, deferred to IPI-639 proper or IPI-437 UI** — `insertAssetEvent` without `actorId` cannot satisfy `who/when/why/version` contract (`cloudinary-plan.md:98`)

**Why no `asset_approvals` for v1 provider moderation:** `moderation_status` + `asset_events` (`kind: moderated`) already gives exact-version provider audit with `request_id` composite idempotency and `is_org_member` RLS. `asset_approvals` with `actorId` is for **business** approval, not provider moderation — defer until business `approved ≠ published` gate is proven.

**4 hardenings applied in this branch (beyond 5-step proof):**
- `upsertCloudinaryAssetRecord` now `moderation_status: pending` on `isNewerVersion` overwrite (approved v1 → v2 pending, not inherited)
- `handle_moderation_event` atomic via `public.handle_moderation_event` RPC (update + insert in one transaction, `request_id` composite, `23505` idempotent, `now()` in fallback distinguishes `approved→rejected→approved` same version)
- `asset_events` for moderation is `kind: moderated` (provider, `reason: manual`) — business `approved` will be separate with `actorId`

## 5-Step Production Proof (live `dzqy2ixl0` `ipix/test-moderation-1786512340344`)

1. **Upload through iPix** (`asset_folder: ipix/dev/a8f00853.../03720393.../products`, `context: env=dev|org_id=...|brand_id=...`, `moderation: manual`, `upload_preset: ipix-signed-upload`, `type: authenticated`) → Cloudinary `moderation: pending` `asset_id 77b275d..., version 1786512340` → Supabase `cloudinary_assets` `moderation_status: pending` `status: ready` + `asset_events` `kind:upload` `request_id:98278...` (`upload` webhook, exact `cloudinary_asset_id`+`version`)
2. **Approve exact version** via `cloudinary.api.update(publicId, {moderation_status:'approved', type:'authenticated'})` → Cloudinary `moderation approved` same `asset_id`+`version`
3. **Supabase mirror becomes approved** → `select moderation_status` where `public_id='ipix/test-moderation-...'` → `approved` `version 1786512340` (where `cloudinary_asset_id`+`version` exact, not `public_id` alias)
4. **One matching audit + idempotency** → `select kind,request_id,version from asset_events where cloudinary_asset_id='77b275...'` → 2 rows `upload` + `approved` (`request_id 77b275...:1786512340:approved`), re-`api.update` same `approved` → still 2 rows (composite `(request_id,asset_id)` 23505, no duplicate)
5. **Org isolation** → `cloudinary_assets` + `asset_events` RLS `is_org_member(b.org_id)` — brand `03720393...` org `a8f00853...` only visible to that org (verified `pg_policies` `asset_events_select` org-aware, `select` 0 for other org)

**Version safety:** overwrite same `public_id` → new `version 1786512424` `moderation pending` in Cloudinary, Supabase `overwrite` event `version 1786512424` `request_id 708d2b...` — `v1 approved` history unchanged, `v2 pending` until separately approved (proven `asset_events` 3 rows: `upload` `approved` `overwrite`)

## Why not `asset_approvals` for v1

- `moderation_status` (`pending/approved/rejected`) + `cloudinary_asset_id`+`version` + `asset_events` (`request_id` unique, `kind` moderated/approved/rejected) already gives exact-version audit, idempotency, and brand isolation
- `asset_approvals` would duplicate `moderation_status` without new business rule (e.g., `approved` ≠ `published` publish gate) — defer until such gate is proven required
- Reuse 95%: preset `ipix-signed-upload` + `sign-upload` `api_sign_request` + webhook `mapProviderIdentity` + `version` stale guard + `is_org_member` RLS

## Tests

- `webhook` `moderation approved` + `sign-upload` `moderation manual` → **90 pass** (70 webhook + 8 asset-events + 12 sign-upload) on `main` `47ca470d7`
- `supabase:verify-rls` pass (3 hyperdrive infra fails only), `tsc` 0, `git diff --check` 0

## Rollback

Remove `moderation` trigger `additive:true` from `https://www.ipix.co/api/assets/cloudinary/webhook` if needed — no schema rollback (no new table).

## Done Gate

**Merged** = `moderation` trigger + webhook + `moderation_status` write already on `main` via IPI-64 `81aba5f04`. **Verified** = 5-step proof above on `main` `47ca470d7` with exact `cloudinary_asset_id`+`version` + org isolation + idempotency.
