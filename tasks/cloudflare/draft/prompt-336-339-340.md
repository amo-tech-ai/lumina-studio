Review and audit these pull requests in order:

1. **IPI-468 · SEC-001 — Add Bearer Token Authentication to Worker Gateway**
   PR #339
   https://github.com/amo-tech-ai/lumina-studio/pull/339

2. **IPI-525 · CF-AI-011 — Enable Workers AI Tool Calling and Update Model Registry**
   PR #340
   https://github.com/amo-tech-ai/lumina-studio/pull/340

3. **IPI-525 · AUDIT-001 — Workers AI Tool Calling Audit Documentation**
   PR #336
   https://github.com/amo-tech-ai/lumina-studio/pull/336

Use the relevant GitHub, Cloudflare, Sentry, browser, testing, and repository skills/MCP tools. Verify all technical claims against current official documentation and the current `main` branch. Do not trust PR descriptions, comments, test summaries, or previous audits without direct proof.

## Stage 1 — Resolve Scope and Branch State

For each PR:

* Confirm branch, HEAD SHA, base SHA, mergeability, CI status, review status, unresolved threads, and changed files.
* Compare the PR branch against current `origin/main`.
* Detect inherited commits, stale branches, duplicate changes, mixed concerns, unrelated lockfile churn, and changes already merged elsewhere.
* Enforce one concern per PR.
* Stop and report immediately if the checked-out branch does not match the PR being audited.

## Stage 2 — Official Documentation Verification

Verify relevant claims using official primary sources only:

* Cloudflare Workers AI model availability, deprecation, function calling, streaming, authentication, secrets, Service Bindings, and error behavior.
* GitHub Actions and PR-Agent workflow behavior where relevant.
* Sentry findings and review comments.
* Repository rules in `AGENTS.md`, `CLAUDE.md`, skills, and task specifications.

Create a source table:

| Claim | Official source | Verified? | Correction |
| ----- | --------------- | --------: | ---------- |

Do not use assumptions or outdated model documentation.

## Stage 3 — Code and Security Audit

Audit each changed file for:

* correctness
* security
* fail-open behavior
* authentication bypasses
* missing validation
* incorrect HTTP status codes
* unsupported model capabilities
* fallback failures
* silent error handling
* malformed streaming behavior
* secret exposure
* missing request limits
* regression risk
* unnecessary complexity

For **IPI-468 · SEC-001 — Add Bearer Token Authentication to Worker Gateway**, verify:

* missing or malformed tokens are rejected
* production fails closed when secrets are absent
* local bypass requires an explicit development flag
* `/health` remains accessible
* auth runs before body parsing and provider calls
* client errors return `400`
* unauthorized requests return `401`
* configuration failures return an appropriate `5xx`
* upstream failures remain distinguishable
* secrets are configured through Wrangler/Infisical and never committed

For **IPI-525 · CF-AI-011 — Enable Workers AI Tool Calling and Update Model Registry**, verify:

* the exact model is active and officially supports tool calling
* the registry capability metadata is accurate
* partial `MODEL_REGISTRY_OVERRIDE` values preserve unspecified default tiers
* Gemini no-op tool fields are handled correctly
* active tool requests fail closed on unsupported providers
* `tools`, `tool_choice`, `tool_calls`, `tool_call_id`, and streaming deltas remain compatible
* model-registry changes and Gemini-provider fixes are separated if they are independent concerns

For **IPI-525 · AUDIT-001 — Workers AI Tool Calling Audit Documentation**, verify:

* every statement matches current code and current deployment state
* stale findings are removed
* scores are evidence-based
* “production ready” claims match actual blockers
* links, task IDs, PR numbers, model names, and test counts are current

## Stage 4 — Tests and Real-World Verification

Run all relevant tests:

```bash
cd services/cloudflare-worker
npm test
npm run typecheck
npm run lint
```

Run app tests only where the PR can affect the app:

```bash
cd app
npm test
npm run typecheck
npm run lint
npm run build
```

Add missing focused tests before recommending merge.

Run real-world user journeys:

### Authentication journey

```text
Missing token → 401
Wrong token → 401
Malformed scheme → 401
Valid token → request reaches provider
Missing production secret → fail closed
Explicit local bypass → allowed only locally
GET /health → 200
Validation error → 400
Provider failure → correct 5xx
```

### Tool-calling journey

```text
Mastra/client
→ Worker gateway
→ fast tier
→ Workers AI
→ tool_calls returned
→ tool executes
→ tool result submitted
→ final model response
```

Test both normal and streaming requests.

Capture:

* HTTP status
* provider
* resolved model
* request ID
* response shape
* fallback behavior
* tool-call output
* error body

Do not report a live test as passed unless it was actually executed.

## Stage 5 — Review Threads and CI

* Inspect every unresolved review thread.
* Confirm whether the fix exists in the current HEAD.
* Reply with commit evidence where appropriate.
* Do not mark a finding resolved only because tests pass.
* Distinguish required checks, advisory checks, unrelated scanner findings, and genuine blockers.
* Do not bypass Codacy, Sentry, or security findings without written evidence that they are unrelated or fixed.

## Stage 6 — Scoring

Score every PR using this system:

| Category                | Weight |
| ----------------------- | -----: |
| Specification accuracy  |    15% |
| Code correctness        |    20% |
| Security and safety     |    20% |
| Test coverage           |    15% |
| Real-world verification |    15% |
| Scope discipline        |    10% |
| Documentation           |     5% |

Use:

* 🟢 90–100: merge-ready
* 🟡 70–89: mostly correct, fixes required
* 🔴 0–69: blocked
* ⚪ not started or no evidence

For each PR, report:

* overall score
* percent complete
* production-readiness percentage
* probability of successful deployment
* whether the task will succeed
* whether it is safe to merge
* confidence level

## Stage 7 — Required Report

Generate one concise audit report with:

### Executive summary

| PR | Task | Score | Production readiness | Verdict |
| -- | ---- | ----: | -------------------: | ------- |

### Findings by PR

| Severity | Finding | Evidence | Impact | Required correction |
| -------- | ------- | -------- | ------ | ------------------- |

### Missing items

List anything absent from the specification, tests, deployment setup, monitoring, rollback plan, security controls, or documentation.

### Corrected execution order

State whether to:

1. finish and merge PR #339
2. split or correct PR #340
3. continue PR #336 in a fresh worktree

Explain dependencies.

### Exact corrections

Provide a checklist for each PR using the format:

```text
IPI-XXX · TASK-ID — Full Task Name
```

### Final verdict

Answer clearly:

* Is each PR correct?
* Is each PR production-ready?
* Will each task succeed?
* What blocks merge?
* What must be fixed first?
* What evidence is still missing?

Do not merge, close, dismiss, rebase, force-push, or resolve threads automatically. Make corrections only when explicitly asked. Report all commands run, files changed, commits inspected, test results, live-test evidence, and remaining blockers.
