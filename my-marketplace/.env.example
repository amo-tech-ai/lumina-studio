# Mercur monorepo — shared secrets (Infisical path: /mercur)
# Source of truth: Infisical project secret-management → dev → /mercur
# Run: infisical run --path=/mercur -- yarn dev  (from repo root, use --recursive if needed)

STRIPE_API_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
